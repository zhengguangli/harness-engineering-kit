# agents/

本 skill 不配对独立执行 agent。

原因:编排是"主对话需要持续记住才能推理"的路由知识,不是"可委派出去等结果"的执行任务。根据 `harness-authoring` 技能的指导——"如果这件事需要主对话'记住'才能继续推理,用 skill;如果这件事可以'派出去、等结果',用 subagent"——编排属于前者。

编排逻辑由主对话直接根据 `SKILL.md` 中的工作流和决策树执行。
